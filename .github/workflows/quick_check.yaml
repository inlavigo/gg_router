# Runs the gg quick check on every push and pull request against main.

name: Tests

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    name: Quick Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      # Uses the Node.js preinstalled on the runner image.
      - run: npx @tssuite/ggwsm one did commit
